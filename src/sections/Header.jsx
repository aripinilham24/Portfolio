import Navbar from "../component/Navbar";

const Header = () => {
    return (
        <header className="sticky top-0 bg-dark z-10">
            <Navbar />
        </header>
    );
};

export default Header;